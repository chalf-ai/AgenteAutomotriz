# Copia este archivo a .env y rellena las claves (no subas .env a git)
# cp .env.example .env

# ----- API OpenAI (obligatorio para el agente) -----
# Pega aquí tu API key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4o-mini

# ----- WhatsApp Business (Meta Cloud API) -----
# Los clientes escribirán por WhatsApp; aquí van las claves de tu app en developers.facebook.com
# Crear app en: https://developers.facebook.com/apps → WhatsApp → API Setup

# Token de acceso permanente (o temporal para pruebas)
WHATSAPP_ACCESS_TOKEN=

# ID del número de teléfono desde el que respondes (en API Setup → "From" phone number)
WHATSAPP_PHONE_NUMBER_ID=

# (Opcional) ID de la cuenta de negocio de WhatsApp
# WHATSAPP_BUSINESS_ACCOUNT_ID=

# Token que tú eliges para que Meta verifique el webhook (pon el mismo en la configuración del webhook)
WHATSAPP_WEBHOOK_VERIFY_TOKEN=

# ----- Stock y datos -----
STOCK_FILE=data/stock.csv
STOCK_DB_PATH=data/stock.db
FAQ_CACHE_PATH=data/faq_cache.db
